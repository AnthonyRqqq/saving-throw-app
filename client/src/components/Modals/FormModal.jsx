import { Modal, Button, Form } from 'react-bootstrap'

export default function FormModal({ show, onHide, style, onClick}) {
    <Modal className='centeredModal' show={show} onHide={onHide} style={style}></Modal>
}